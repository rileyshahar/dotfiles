#!/usr/bin/env bash
set -euo pipefail
# cmd+C: open the project in Claude Cowork via deep link.
dir="${1:?usage: cowork <project-dir>}"
encoded="$(jq -rn --arg x "$dir" '$x | @uri')"
open "claude://cowork/new?folder=$encoded"
